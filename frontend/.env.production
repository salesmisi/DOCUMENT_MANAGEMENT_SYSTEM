VITE_API_URL=https://adequate-inspiration-production-1a50.up.railway.app/api
# Leave VITE_AGENT_URL unset in production unless you expose the local agent through
# a secure tunnel (ngrok/cloudflared) or a public agent endpoint.